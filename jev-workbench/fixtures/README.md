# 합성 응답

모든 `*.response.json`은 실행기와 스킬 흐름 검사용으로 직접 만든 값이다. Jev API를 호출하여 얻은 응답이 아니다. 첫 Choice 후보에 0.7을 주며 confidence는 설명용 0.4다. 이 관계를 실제 확신도 공식으로 해석하지 않는다. Score는 균등 분포, Noul은 설명용 0.8이다.

`eval-cases.json`은 집계기가 성공과 실패를 모두 세는지 확인하기 위해 의도적으로 2개 중 1개만 라벨에 맞도록 구성했다. 이 50%는 Jev 모델의 성능이 아니다.
