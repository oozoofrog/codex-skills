interface:
  display_name: "GPT Pro Collaborator"
  short_description: "Desktop ChatGPT Pro with read-only repository context"
  default_prompt: "Use $gptpro for this explicitly requested ChatGPT Pro consultation. Prepare the smallest bounded read-only repository package, use the exact installed MCP companion and visible macOS ChatGPT new general Chat with Pro, send at most once, collect the bound response, and independently validate all advice."
policy:
  allow_implicit_invocation: false
