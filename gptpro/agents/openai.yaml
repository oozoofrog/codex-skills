interface:
  display_name: "GPT Pro Collaborator"
  short_description: "GitHub-first ChatGPT Pro handoffs with human takeover"
  default_prompt: "Use $gptpro in review mode, initialize local handoff storage if needed, prepare a GitHub-first approved repository handoff with text fallback, consult ChatGPT Pro with human takeover at trust or browser boundaries when needed, and validate the advisory response."
