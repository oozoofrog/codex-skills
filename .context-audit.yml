strict: true

checks:
  require_root_claude: true
  allow_nested_claude: false

line_limits:
  CLAUDE.md:
    hint: 150
    error: 200
  CONTEXT.md:
    hint: 120
  AGENTS.md:
    hint: 120

max_code_block_lines: 80
