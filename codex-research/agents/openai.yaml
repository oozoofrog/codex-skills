interface:
  display_name: "Codex Research"
  short_description: "Resume or inspect legacy .codex-research runner state"
  default_prompt: "Inspect an existing .codex-research workspace, run status/reconcile/resume safely, and report hard gate, experiment status, control action, and remaining budget."
policy:
  allow_implicit_invocation: false
