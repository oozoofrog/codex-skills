# 수동 병합용 AGENTS.md 조각

아래를 검토한 뒤 기존 파일과 충돌하지 않는 위치에 수동 병합한다. 설치기는 AGENTS.md를 수정하지 않는다.

```markdown
## Jev decision workflows

Use the installed `jev-workbench` skill only for explicitly requested Jev work or delegated decision points defined by the user/project. Distinguish delegated selection from advisory review and evaluation. Do not replace the current Codex model/provider or bypass existing approvals.

Preserve user requirements, mandatory tests, observed evidence and live/fixture provenance. A decision receipt is not execution permission. Submit new evidence through a linked packet rather than asking repeatedly until a preferred answer appears. Retain only task/packet references and unresolved evidence in session handoffs.
```
